---
type: handoff
title: "Phase 3b Vercel Sandbox driver — manual UAT in progress"
timestamp: 2026-07-06T16:58:30Z
branch: feat/deployments-phase-3b.md
base_sha: c6941bb5
head_sha: cb994e4b1
---

# Handoff: Phase 3b Vercel Sandbox driver — manual UAT in progress

## What this session was

Implementing `docs/specs/2026-07-04-kata-environments-deployments-phase-3b-plan.md` via the `plan-build-verify` skill. The Build phase is complete (M1–M6 committed). An independent `reviewer` subagent ran adversarial UAT; three findings were fixed. The session ended during **manual UAT** by the maintainer (the user), who hit a UI confusion issue (now fixed) and is rate-limited on a Vercel Hobby plan.

A fresh agent should pick up at the manual UAT step. Do not restart Build.

## Where to find everything

- **Spec (source of truth, status: Implemented):** `docs/specs/2026-07-04-kata-environments-deployments-phase-3b-design.md` — 13 acceptance criteria AC-3b.1..13
- **Plan + Build completion report:** `docs/specs/2026-07-04-kata-environments-deployments-phase-3b-plan.md` (report appended at the end)
- **Implementation plan copy:** `docs/specs/plans/2026-07-06-phase-3b-vercel-sandbox-implementation-plan.md`
- **Adversarial UAT output:** `.pi-subagents/artifacts/3c834eaf_reviewer_0_output.md`
- **Deferred work entry filed:** `docs/specs/deferred-work.md` (search "durable RunningSession reclamation")
- **ADR 0007:** `docs/adrs/0007-vercel-sandbox-first-cloud-sandbox-driver.md`
- **Vercel Sandbox SDK skill:** `.agents/skills/vercel-sandbox/SKILL.md` (SDK v2 usage patterns)

Read the Build completion report's AC evidence map and the "Known follow-up issues / deferrals" section before doing anything.

## Current branch state

```
c6941bb5 (base) .. cb994e4b1 (head)
```

11 commits. Worktree clean. All gates green:

- `vp run typecheck` — 0 errors
- `vp run test` — 1347 passed, 7 skipped, 0 failed (sandbox-vercel: 20 passed / 4 skipped credentialed)
- `vp check` — 0 errors, formatting clean

Do not re-run the full `vp run test` unless needed (~8 min). The sandbox-vercel package tests (`cd packages/sandbox-vercel && pnpm test`) are the fast confirmation.

## AC status (what's verified vs blocked)

- **Pass (unit/contract-gated):** 3b.1, 3b.5, 3b.13; 3b.4/3b.6/3b.7/3b.10 at unit level
- **Blocked (maintainer-local credentialed, by spec design):** 3b.3, 3b.8, 3b.9, 3b.11, 3b.12 — require `E2E_VERCEL_*` trio + recorded UAT. The maintainer is on a Vercel Hobby plan and is rate-limited, so these run one-at-a-time, not via the batched test suite.
- **3b.11 (Claude sign-in)** is the unverified spike: `claude setup-token` under `script(1)`. Architecture complete; `PROVIDER_LOGIN_SPECS` in `apps/server/src/sandbox/providerLogin.ts` holds the assumed command/path. If the spike fails, adjust the **data** in that table (not the architecture) and record findings in `uat-evidence/.../notes.md`.

## Where the maintainer is in manual UAT

The maintainer ran `pnpm run dev:desktop` and was walking Settings → Environments. They hit two issues, both now fixed in `cb994e4b1`:

1. **Two identical, unlabeled env-var editors** — the card renders `ProviderEnvironmentSection` twice: the target-level one (where the Vercel trio belongs) and the repo-level one inside `SavedEnvironmentEditor`. The maintainer entered the trio in the wrong one (repo-level, under Terminals), so `validate` failed with "Set VERCEL_TOKEN, VERCEL_TEAM_ID and VERCEL_PROJECT_ID as sensitive environment variables". Fixed by adding `title`/`description` props to `ProviderEnvironmentSection` and labeling them:
   - **Runtime environment variables** (top, below vCPUs) — "Apply to every session on this target. Add VERCEL_TOKEN, VERCEL_TEAM_ID, and VERCEL_PROJECT_ID here as sensitive variables."
   - **Repository environment variables** (under Terminals) — "Apply only when starting a session for this repository (merged on top of the target's runtime variables)."

2. The maintainer needs to restart `pnpm run dev:desktop` to pick up the label fix, then re-enter the trio in the **Runtime environment variables** section and retry **Test connection**.

The maintainer is on a Vercel **Hobby** plan and gets rate-limited (429) when firing multiple provisions in quick succession (e.g. the batched integration tests). Manual UAT must be paced: one provision at a time, wait between attempts.

## Credentials

The maintainer created a throwaway Vercel project `kata-sandbox-uat` and put the trio in a gitignored `./.env` as **`E2E_VERCEL_*`** (namespaced to avoid colliding with `VERCEL_*` used for real dev). The harness (`e2e/src/harness/env.ts` `readVercelCredentials`) and integration test (`packages/sandbox-vercel/src/VercelSandboxProvider.integration.test.ts`) read `E2E_VERCEL_TOKEN`/`E2E_VERCEL_TEAM_ID`/`E2E_VERCEL_PROJECT_ID`. The **driver and product** still use `VERCEL_*` (configured per deployment target in Settings).

`.env` is gitignored (confirmed: `.gitignore` line 11 + `.env*` line 34). Do not commit it.

For the manual UI walkthrough, the trio goes in the deployment target's Runtime env vars as `VERCEL_TOKEN`/`VERCEL_TEAM_ID`/`VERCEL_PROJECT_ID` (not the `E2E_` prefix — that's test-harness only).

## Manual UAT instructions (resume here)

The full manual UAT script is in the conversation and summarized in the Build completion report's deferrals. Paced version for a rate-limited Hobby plan:

1. `cd /Volumes/EVO/orca-workspaces/kata-code/deployments-phase-3b.md && pnpm run dev:desktop`
2. Settings → Environments → Add sandbox environment → Driver: **Vercel Sandbox**, label "UAT".
3. Expand the card → **Runtime environment variables** section (top, below vCPUs) → add `VERCEL_TOKEN`, `VERCEL_TEAM_ID`, `VERCEL_PROJECT_ID` as sensitive vars with the real values.
4. **Test connection** (one provision) → expect `validate: ok` → `provision: ok` → `dispose: ok` → `done: ok`. If 429, wait and retry.
5. **Start session** → expect `Session ready: https://<sandbox>.vercel.run` + `Expires in Xm` countdown (AC-3b.5/3b.8).
6. **Extend** → countdown bumps (AC-3b.5).
7. **Snapshot** → confirm "Snapshotting pauses the sandbox" copy → `Lapsed` + snapshot id (AC-3b.7).
8. **Resume** → card returns to `Session ready` (AC-3b.6).
9. **Sign in Claude** (AC-3b.11 spike) → dialog shows OAuth URL → complete in browser → paste code → **Submit code** → "Credential stored" toast + dialog closes. **This is the unverified spike.** If `claude setup-token` doesn't work, try `claude` login. Record which command + credential path worked.
10. **Dispose** → session line disappears (AC-3b.12). On a second paired client, confirm unreachable (AC-3b.9).
11. **Stored credential round-trip** (AC-3b.10) → after step 9, Start a fresh session → Claude reports authenticated without an env-var key.

Evidence: `mkdir -p uat-evidence/phase-3b-$(date +%Y%m%d-%H%M%S)/{screenshots,logs}`. `uat-evidence/` is not gitignored — don't commit credentials.

## Open follow-ups (not blocking UAT)

- **M5 spike UAT** (step 9 above) — may require updating `PROVIDER_LOGIN_SPECS` data in `apps/server/src/sandbox/providerLogin.ts`.
- **`RunningSession` non-durability** — filed in `docs/specs/deferred-work.md`; no code change needed now.
- **`testConnection` for a Vercel target provisions a real, billed sandbox** — card copy note is a follow-up (build report risk #7).
- **e2e gate** (`vp run e2e --project desktop-dev --grep @environments-deploy`) — not run locally; requires dev stack + Docker + `katacode:local`. The Vercel spec is skip-guarded so CI stays green uncredentialed.

## Adversarial UAT findings (all fixed in `664f79101`)

- AF-1 (medium): `ProviderSignInDialog` didn't close on `accepted: true` (submit is unary, not a stream event) — fixed; dead stream `success`/`invalid-code` cases removed.
- AF-2 (low): dead ternary in `createSnapshot` (both branches `{ expiration: 0 }`) — simplified.
- AF-3 (low): `RunningSession` non-durability not filed in `deferred-work.md` — filed.

Adversarial checks the subagent confirmed pass: `mergeVercelAuthIntoConfig` wraps every `materializeOne`; keepalive `onLapse` mutates the record surfaced by `toSummary`; `createSessionSnapshot` marks lapsed for vercel kind; Dockerfile fail-loud fires for non-docker drivers; all 5 new RPCs registered in `WsRpcGroup`; auth env vars excluded from `Sandbox.create`; `describe()` advertises `supportsResume: true` + public reachability.

## Key files (if the fresh agent needs to make changes)

- `packages/sandbox-vercel/src/VercelSandboxProvider.ts` — the driver
- `packages/sandbox-vercel/src/{config,sdk,bootstrap}.ts`
- `apps/server/src/sandbox/SandboxService.ts` — registration, lifecycle, Connect
- `apps/server/src/sandbox/{sessionKeepalive,providerLogin,storedSandboxCredentials,credentialSeed}.ts`
- `apps/web/src/components/settings/SandboxDeploymentSettings.tsx` — deployment target UI
- `apps/web/src/components/settings/ProviderSignInDialog.tsx` — sign-in flow UI
- `apps/web/src/components/settings/ProviderInstanceCard.tsx` — `ProviderEnvironmentSection` (now has title/description props)
- `e2e/tests/environments-deploy/vercel-deploy.spec.ts` — credentialed e2e (skip-guarded)

## Suggested skills

- `plan-build-verify` — this session is in the **Verify** phase; read `references/verify.md` and `references/user-acceptance/workflow.md` for the acceptance-evidence format. The Build phase is done; do not re-enter Build unless a deferral requires code changes.
- `vercel-sandbox` — SDK v2 usage and credential patterns if the spike UAT or a driver adjustment is needed.
- `user-acceptance` — for structuring the recorded UAT evidence (screenshots, logs, notes.md, the acceptance-criteria matrix).
- `playwright-cli` — only if running the e2e gate (`vp run e2e --project desktop-dev --grep @environments-deploy`); requires stopping `pnpm run dev` first per AGENTS.md.

## What to do first

1. Read the Build completion report at the end of `docs/specs/2026-07-04-kata-environments-deployments-phase-3b-plan.md`.
2. Read `.pi-subagents/artifacts/3c834eaf_reviewer_0_output.md` (the adversarial UAT).
3. Check with the maintainer whether they've resumed the manual UAT after the label fix (`cb994e4b1`) and what passed/failed, especially the Claude sign-in spike (AC-3b.11).
4. If the maintainer completed UAT, record the verdict and update the Build completion report. If the spike found `claude setup-token` doesn't work, update `PROVIDER_LOGIN_SPECS` data and re-run the sign-in step.
5. Do not mark `GO` — the human grants acceptance per the user-acceptance workflow.
